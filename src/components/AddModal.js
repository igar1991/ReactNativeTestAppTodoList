import React, {useState} from 'react';
import { StyleSheet, TextInput, Button, View, Alert, Modal } from 'react-native';

export const AddModal = ({}) => {

    return (
        <Modal>
            <View>
                
            </View>
        </Modal>

    )
}

const styles = StyleSheet.create({})
